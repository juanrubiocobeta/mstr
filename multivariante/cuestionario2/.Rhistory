library(ade4)
library(ade4)
data(doubs)
library(ade4)
download(ade4)
install.packages("ade4")
library(ade4)
install.packages("ade4")
library(ade4)
install.packages("ade4")
library(ade4)
data(doubs)
datos=doubs$env
View(datos)
matriz_cor <- cor(datos)
# 5. Para responder la pregunta, aislamos las correlaciones de la variable 'bdo'
# y las ordenamos de mayor a menor para ver las más altas.
correlaciones_bdo <- matriz_cor["bdo", ]
print(sort(correlaciones_bdo, decreasing = TRUE))
# (Opcional) Para una visualización gráfica, se puede usar un gráfico de correlaciones
install.packages("corrplot")
library(corrplot)
corrplot(matriz_cor, method = "number", tl.cex = 0.7)
View(datos)
View(datos)
View(datos)
# 1. Cargar la librería necesaria y el conjunto de datos
# Si no está instalada, primero ejecutar: install.packages("ade4")
library(ade4)
data(doubs)
# 2. Asignar el data frame 'env' a una nueva variable
datos <- doubs$env
# 3. Realizar el Análisis de Componentes Principales (ACP)
# Es fundamental escalar los datos ("scale. = TRUE") porque las variables
# están medidas en diferentes unidades y rangos (ej: altitud vs. pH).
# Esto hace que el ACP se base en la matriz de correlaciones.
pca_resultado <- prcomp(datos, scale. = TRUE)
# 4. Obtener el resumen del análisis para ver la inercia (varianza) explicada
summary_pca <- summary(pca_resultado)
# 5. Mostrar la tabla de importancia de las componentes, que contiene la
# proporción de varianza y la proporción acumulada.
print(summary_pca$importance)
# 5. Mostrar la tabla de importancia de las componentes, que contiene la
# proporción de varianza y la proporción acumulada.
print(summary_pca$importance)
0.86886-0.777570
# 1. Cargar librerías. Usaremos 'FactoMineR' porque ofrece una salida
#    muy clara para las contribuciones.
# Si no está instalada, primero ejecutar: install.packages("FactoMineR")
library(ade4)
library(FactoMineR)
install.packages("FactoMineR")
library(FactoMineR)
# 2. Cargar el conjunto de datos 'doubs'
data(doubs)
# 3. Asignar el data frame 'env' (variables medioambientales) a una nueva variable
datos <- doubs$env
# 4. Realizar el Análisis de Componentes Principales (ACP)
#    scale.unit=TRUE es para basar el análisis en la matriz de correlaciones.
#    graph=FALSE evita que se generen los gráficos automáticamente.
pca_resultado <- PCA(datos, scale.unit = TRUE, graph = FALSE)
# 5. Obtener las contribuciones de los individuos (filas) a las componentes.
#    Las contribuciones se expresan en porcentaje.
contribuciones_individuos <- pca_resultado$ind$contrib
# 6. Mostrar las contribuciones de las primeras componentes para el individuo 25
print(contribuciones_individuos[25, ])
# 7. (Opcional) Verificar qué individuo contribuye más a la primera componente (Dim.1)
# which.max() nos da la posición del valor máximo.
fila_max_contrib_dim1 <- which.max(contribuciones_individuos[, "Dim.1"])
valor_max_contrib_dim1 <- max(contribuciones_individuos[, "Dim.1"])
print(paste("El individuo que más contribuye a la Dim.1 es el:", fila_max_contrib_dim1))
print(paste("Su contribución es de:", round(valor_max_contrib_dim1, 2), "%"))
# 4. Realizar el Análisis de Componentes Principales (ACP)
#    scale.unit=TRUE es para basar el análisis en la matriz de correlaciones.
#    graph=FALSE evita que se generen los gráficos automáticamente.
pca_resultado <- PCA(datos, scale.unit = TRUE, graph = FALSE)
# 5. Obtener las contribuciones de las variables (columnas) a las componentes.
#    Las contribuciones se expresan en porcentaje.
contribuciones_variables <- pca_resultado$var$contrib
# 6. Mostrar la tabla completa de contribuciones de las variables
print(contribuciones_variables)
# 7. Extraer y mostrar específicamente la contribución de 'dfs' a la primera dimensión
contrib_dfs_dim1 <- contribuciones_variables["dfs", "Dim.1"]
print(paste("La contribución de 'dfs' a la Dim.1 es:", round(contrib_dfs_dim1, 4), "%"))
# 4. Realizar el Análisis de Componentes Principales (ACP)
#    scale.unit=TRUE es para basar el análisis en la matriz de correlaciones.
#    graph=FALSE evita que se generen los gráficos automáticamente.
pca_resultado <- PCA(datos, scale.unit = TRUE, ncp = 5, graph = FALSE)
# 5. Crear el gráfico de variables (círculo de correlaciones)
#    Este gráfico es el que se necesita para responder la pregunta.
fviz_pca_var(pca_resultado, repel = TRUE)
library(FactoMineR)
library(factoextra)
install.packages((factoextra))
install.packages("factoextra")
library(factoextra)
# 2. Cargar el conjunto de datos 'doubs'
data(doubs)
# 3. Asignar el data frame 'env' a una nueva variable
datos <- doubs$env
# 4. Realizar el Análisis de Componentes Principales (ACP)
#    scale.unit=TRUE es para basar el análisis en la matriz de correlaciones.
#    graph=FALSE evita que se generen los gráficos automáticamente.
pca_resultado <- PCA(datos, scale.unit = TRUE, ncp = 5, graph = FALSE)
# 5. Crear el gráfico de variables (círculo de correlaciones)
#    Este gráfico es el que se necesita para responder la pregunta.
fviz_pca_var(pca_resultado, repel = TRUE)
# 2. Cargar el conjunto de datos 'doubs'
data(doubs)
# 3. Asignar el data frame 'env' a una nueva variable
datos <- doubs$env
# 4. Realizar el Análisis de Componentes Principales (ACP)
pca_resultado <- PCA(datos, scale.unit = TRUE, ncp = 5, graph = FALSE)
# 5. Crear el gráfico combinado de variables e individuos (biplot)
fviz_pca_biplot(pca_resultado, repel = TRUE)
